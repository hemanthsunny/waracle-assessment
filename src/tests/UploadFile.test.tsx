import React from "react";
import { render, fireEvent, waitForElement } from "@testing-library/react";

import UploadFileComponent, { Props } from "../components/upload_file/component";

describe("<UploadFileComponent />", () => {
  test("should display a upload file page, with an option to select a file by default", async () => {
    // ???
  });
});
